name: brain-config-scan

on:
  pull_request:
  push:
    branches: [main]

jobs:
  scan:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run brain config leak scan
        run: bash scripts/check-brain-config-leaks.sh --strict
